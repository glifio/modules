{
  "name": "@glif/logger",
  "version": "2.0.0-beta.7",
  "description": "Handles logging for GLIF apps, modules and components",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "../../node_modules/.bin/rimraf dist/* && mkdirp dist",
    "build-babel": "../../node_modules/.bin/babel ./src/ --out-dir ./dist/ --extensions \".ts,.tsx\" --source-maps --ignore '**/__tests__'",
    "build-types": "../../node_modules/.bin/tsc --emitDeclarationOnly",
    "build": "npm run clean && npm run build-types && npm run build-babel",
    "lint": "tslint -p tsconfig.json",
    "prepublishOnly": "npm run build"
  },
  "author": "Infinite Scroll <squad@infinitescroll.org> (https://infinitescroll.org)",
  "license": "(Apache-2.0 OR MIT)",
  "dependencies": {
    "@sentry/browser": "^6.17.2",
    "@sentry/tracing": "^6.17.3"
  },
  "devDependencies": {
    "babel-eslint": "10.1.0",
    "prettier": "2.6.2",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0"
  },
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/glifio/modules/tree/primary/packages/logger"
  }
}
